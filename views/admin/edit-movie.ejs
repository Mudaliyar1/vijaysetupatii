
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white font-sans">
    <header class="bg-gray-800 py-4 shadow-lg">
        <div class="container mx-auto px-4 flex justify-between items-center">
            <h1 class="text-3xl font-bold text-white">Edit Movie</h1>
            <nav class="flex items-center space-x-4">
                <a href="/admin/movies" class="bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded">Back to Movies</a>
            </nav>
        </div>
    </header>

    <main class="container mx-auto px-4 py-8">
        <form action="/admin/movies/<%= movie._id %>?_method=PUT" method="POST" class="max-w-2xl mx-auto bg-gray-800 rounded-lg shadow-lg p-8">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Same form fields as add-movie.ejs but with movie data pre-filled -->
                <div class="col-span-2">
                    <label class="block text-sm font-medium mb-2">Movie Title</label>
                    <input type="text" name="title" value="<%= movie.title %>" required class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500">
                </div>
                
                <div class="col-span-2">
                    <label class="block text-sm font-medium mb-2">Description</label>
                    <textarea name="description" rows="4" required class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500"><%= movie.description %></textarea>
                </div>

                <div>
                    <label class="block text-sm font-medium mb-2">Genre</label>
                    <input type="text" name="genre" value="<%= movie.genre %>" required class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500">
                </div>

                <div>
                    <label class="block text-sm font-medium mb-2">Release Date</label>
                    <input type="date" name="releaseDate" 
                           value="<%= movie.releaseDate ? movie.releaseDate.toISOString().split('T')[0] : '' %>" 
                           required 
                           class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500">
                </div>

                <div>
                    <label class="block text-sm font-medium mb-2">Duration (minutes)</label>
                    <input type="number" name="duration" value="<%= movie.duration %>" required class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500">
                </div>

                <div>
                    <label class="block text-sm font-medium mb-2">Image URL</label>
                    <input type="url" name="image" value="<%= movie.image %>" class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500">
                </div>

                <div class="col-span-2">
                    <label class="block text-sm font-medium mb-2">Director</label>
                    <input type="text" name="director" value="<%= movie.director %>" required class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500">
                </div>

                <div class="col-span-2">
                    <label class="block text-sm font-medium mb-2">Cast (comma-separated)</label>
                    <input type="text" name="cast" value="<%= movie.cast.join(', ') %>" required class="w-full px-4 py-2 bg-gray-700 rounded-lg focus:ring-2 focus:ring-blue-500">
                </div>
            </div>

            <div class="mt-8 flex justify-end space-x-4">
                <button type="submit" class="bg-yellow-600 hover:bg-yellow-700 text-white px-6 py-2 rounded-lg">
                    Update Movie
                </button>
            </div>
        </form>
    </main>
</body>
</html>
