<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Awards</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white font-sans">
    <%- include('../partials/admin-header', { title: 'Award Management', user: user }) %>

    <main class="container mx-auto px-4 py-8">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold">Awards List</h2>
            <a href="/admin/awards/add" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg flex items-center">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
                </svg>
                Add New Award
            </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <% awards.forEach(award => { %>
            <div class="bg-gray-800 rounded-lg shadow-lg p-6">
                <h3 class="text-xl font-bold mb-2"><%= award.name %></h3>
                <p class="text-gray-400 mb-4"><%= award.description %></p>
                <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-500">
                        <%= award.category %> | <%= award.year %>
                    </span>
                    <div class="space-x-2">
                        <a href="/admin/awards/edit/<%= award._id %>" class="bg-yellow-600 hover:bg-yellow-700 text-white px-4 py-2 rounded">Edit</a>
                        <form action="/admin/awards/<%= award._id %>?_method=DELETE" method="POST" class="inline">
                            <button type="submit" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded">Delete</button>
                        </form>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </main>

    <footer class="bg-gray-800 py-4 mt-8 text-center text-gray-500">
        <p>&copy; 2023 Vijay Sethupathi. All rights reserved.</p>
    </footer>
</body>
</html>